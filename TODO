- Remove DNSJAVA by implementing a minimal DNS client
- Remove harmony/openldap code by implementing a custom sasl package
- Add more application apis (like iterationg over all connections)
- Add more system wide events (connection established, closed, ...)
- Add VCARD support for better contact entries
- Move metadata initialization into a factory
